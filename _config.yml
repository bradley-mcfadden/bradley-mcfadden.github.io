collections:
  projects:
    output:
      true
  posts:
    output:
      true

defaults:
  - scope:
      path: ""
      type: "projects"
    values:
      layout: "project"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

tag_page_layout:
  "tag_page"

tag_page_dir: 
  "tag"